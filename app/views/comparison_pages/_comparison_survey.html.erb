<% survey_questions.each do |question_id, topic| %>
  <a id="question_<%= question_id %>">
    <div class="section-heading">
      <%= topic %>
    </div>
  </a>
  <%= display_survey_question(question_id) %>
  <div class="columns is-multiline">
    <% participants.each do |p| %>
      <div class="column is-one-third">
        <!-- Header -->
        <%= render 'comparison_pages/comparison_participant_header', participant_id: p.id, party_id: p.party_id %>

        <!-- Survey Answer -->
        <p><%= display_survey_answer_text(question_id, p.id) %></p>
      </div>
    <% end %>
  </div>
<% end %>