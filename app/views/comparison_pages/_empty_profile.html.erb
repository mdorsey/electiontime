<%= sanitize empty_profile_text.content %>
<%= mail_to participant_email, "Email " + display_participant(participant_id, true), { target: "_blank", subject: "Complete your profile on ElectionTime.org", class: "button is-primary", role: "button" } %>