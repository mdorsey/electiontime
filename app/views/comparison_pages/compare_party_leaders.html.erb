<% provide(:title, "Compare Party Leaders") %>

<!-- INTRO -->
<%= render 'comparison_pages/comparison_intro', page_heading: 'Compare Party Leaders', participant_title: 'Leaders', display_incumbent: true %>

<!-- NAV MENU -->
<%= render 'comparison_pages/comparison_nav_menu', survey_questions: @survey_questions %>

<!-- BIOGRAPHIES -->
<%= render 'comparison_pages/comparison_biography', participants: @party_leaders %>

<!-- SURVEY QUESTIONS AND ANSWERS -->
<%= render 'comparison_pages/comparison_survey', survey_questions: @survey_questions, participants: @party_leaders %>

<!-- CONTACTS -->
<%= render 'comparison_pages/comparison_contact', participants: @party_leaders %>