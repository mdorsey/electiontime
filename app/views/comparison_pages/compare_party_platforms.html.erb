<% provide(:title, "Compare Party Platforms") %>
<h1>Compare Party Platforms</h1>

<!-- INTRO -->
<%= render 'shared/comparison_intro', participant_title: 'Parties', display_incumbent: false %>

<!-- NAV MENU -->
<%= render 'shared/comparison_nav_menu', survey_questions: @survey_questions %>

<!-- BIOGRAPHIES -->
<%= render 'shared/comparison_biography', participants: @parties %>

<!-- SURVEY QUESTIONS AND ANSWERS -->
<%= render 'shared/comparison_survey', survey_questions: @survey_questions, participants: @parties %>

<!-- CONTACTS -->
<%= render 'shared/comparison_contact', participants: @parties %>