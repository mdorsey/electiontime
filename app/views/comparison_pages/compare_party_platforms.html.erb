<% provide(:title, "Compare Party Platforms") %>

<!-- INTRO -->
<%= render 'comparison_pages/comparison_intro', page_heading: 'Compare Party Platforms', participant_title: 'Parties', display_incumbent: false %>

<!-- NAV MENU -->
<%= render 'comparison_pages/comparison_nav_menu', survey_questions: @survey_questions %>

<!-- BIOGRAPHIES -->
<%= render 'comparison_pages/comparison_biography', participants: @parties, empty_profile_text: @content_empty_profile_text %>

<!-- SURVEY QUESTIONS AND ANSWERS -->
<%= render 'comparison_pages/comparison_survey', survey_questions: @survey_questions, participants: @parties %>

<!-- CONTACT INFORMATION -->
<%= render 'comparison_pages/comparison_contact', participants: @parties %>