<% provide(:title, 'Election Types') %>
<h1>Election Types</h1>

<div class="columns">
  <div class="column is-narrow">
    <%= render 'layouts/admin_menu' %>
  </div>
  <div class="column">
    <div class="buttons">
      <%= link_to '+ New Election Type', new_election_type_path, class: "button is-link" %>
    </div>

    <ul class="resources-list">
      <%= render @election_types %>
    </ul>

    <%= will_paginate renderer: BulmaPagination::Rails, :class => 'is-centered' %>
  </div>
</div>

