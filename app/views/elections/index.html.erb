<% provide(:title, 'Elections') %>
<h1>Elections</h1>

<div class="columns">
  <div class="column is-narrow">
    <%= render 'layouts/admin_menu' %>
  </div>
  <div class="column">
    <div class="buttons">
      <%= link_to '+ New Election', new_election_path, class: "button is-link" %>
    </div>

    <ul class="resources-list">
      <%= render @elections %>
    </ul>

    <%= will_paginate renderer: BulmaPagination::Rails, :class => 'is-centered' %>
  </div>
</div>

