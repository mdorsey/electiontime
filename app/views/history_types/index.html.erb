<% provide(:title, 'History Types') %>
<h1>History Types</h1>

<%= link_to '+ New History Type', new_history_type_path, class: "btn btn-primary" %>
<%= link_to 'Back', admin_resources_path, class: "btn btn-default" %>

<ul class="resources-list">
  <%= render @history_types %>
</ul>

<%= will_paginate %>