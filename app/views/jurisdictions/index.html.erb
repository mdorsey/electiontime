<% provide(:title, 'Jurisdictions') %>
<h1>Jurisdictions</h1>

<div class="columns">
  <div class="column is-narrow">
    <%= render 'layouts/admin_menu' %>
  </div>
  <div class="column">
    <div class="buttons">
      <%= link_to '+ New Jurisdiction', new_jurisdiction_path, class: "button is-link" %>
    </div>

    <ul class="resources-list">
      <%= render @jurisdictions %>
    </ul>

    <%= will_paginate renderer: BulmaPagination::Rails, :class => 'is-centered' %>
  </div>
</div>