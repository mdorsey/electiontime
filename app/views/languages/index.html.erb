<% provide(:title, 'Languages') %>
<h1>Languages</h1>

<div class="buttons">
  <%= link_to '+ New Language', new_language_path, class: "button is-link" %>
  <%= link_to 'Back', admin_resources_path, class: "button" %>
</div>

<ul class="resources-list">
  <%= render @languages %>
</ul>

<%= will_paginate renderer: BulmaPagination::Rails, :class => 'is-centered' %>