<% provide(:title, 'Log Types') %>
<h1>Log Types</h1>

<div class="buttons">
  <%= link_to '+ New Log Type', new_log_type_path, class: "button is-link" %>
  <%= link_to 'Back', admin_resources_path, class: "button" %>
</div>

<ul class="resources-list">
  <%= render @log_types %>
</ul>

<%= will_paginate renderer: BulmaPagination::Rails, :class => 'is-centered' %>