<%= form_for(@participant) do |f| %>
  <%= render 'shared/error_messages', object: f.object %>

  <%= f.label :user_id, "User" %>
  <%= f.select(:user_id, options_for_user, {:prompt => '- Select -'}, {:class => 'form-control'}) %>

  <%= f.label :email, "Email" %>
  <%= f.text_field :email, class: 'form-control' %>

  <%= f.label :phone, "Phone" %>
  <%= f.text_field :phone, class: 'form-control' %>

  <%= f.label :website, "Website" %>
  <%= f.text_field :website, class: 'form-control' %>

  <%= f.label :biography, "Biography" %>
  <%= f.text_field :biography, class: 'form-control' %>

  <%= f.label :is_incumbent, "Is Incumbent" %>
  <%= f.check_box :is_incumbent, class: 'form-control auto-width' %>

  <%= f.label :is_candidate, "Is Candidate" %>
  <%= f.check_box :is_candidate, class: 'form-control auto-width' %>

  <%= f.label :address_id, "Address" %>
  <%= f.select(:address_id, options_for_address, {:prompt => '- Select -'}, {:class => 'form-control'}) %>

  <%= f.label :district_id, "District" %>
  <%= f.select(:district_id, options_for_district, {:prompt => '- Select -'}, {:class => 'form-control'}) %>

  <%= f.label :party_id, "Party" %>
  <%= f.select(:party_id, options_for_party, {:prompt => '- Select -'}, {:class => 'form-control'}) %>

  <%= f.label :leader_participant_id, "Leader Participant" %>
  <%= f.select(:leader_participant_id, options_for_participant, {:prompt => '- Select -'}, {:class => 'form-control'}) %>

  <%= f.submit "Save", class: "btn btn-primary" %>
  <%= link_to 'Cancel', cancel_path, class: "btn btn-default", role: "button" %>
<% end %>