<% provide(:title, 'Participants') %>
<h1>Participants</h1>

<div class="columns">
  <div class="column is-narrow">
    <%= render 'layouts/admin_menu' %>
  </div>
  <div class="column">
    <div class="buttons">
      <%= link_to '+ New Participant', new_participant_path, class: "button is-link" %>
    </div>

    <ul class="resources-list">
      <%= render @participants %>
    </ul>

    <%= will_paginate renderer: BulmaPagination::Rails, :class => 'is-centered' %>
  </div>
</div>