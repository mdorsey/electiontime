<% provide(:title, 'Parties') %>
<h1>Parties</h1>

<div class="columns">
  <div class="column is-narrow">
    <%= render 'layouts/admin_menu' %>
  </div>
  <div class="column">
    <div class="buttons">
      <%= link_to '+ New Party', new_party_path, class: "button is-link" %>
    </div>

    <ul class="resources-list">
      <%= render @parties %>
    </ul>

    <%= will_paginate renderer: BulmaPagination::Rails, :class => 'is-centered' %>
  </div>
</div>

