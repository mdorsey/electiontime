<% provide(:title, 'New Party') %>
<h1>New Party</h1>

<div class="columns">
  <div class="column is-narrow">
    <%= render 'layouts/admin_menu' %>
  </div>
  <div class="column">
    <%= render 'form', party: @party, cancel_path: parties_path %>
  </div>
</div>