<% provide(:title, 'Provinces') %>
<h1>Provinces</h1>

<div class="columns">
  <div class="column is-narrow">
    <%= render 'layouts/admin_menu' %>
  </div>
  <div class="column">
    <div class="buttons">
      <%= link_to '+ New Province', new_province_path, class: "button is-link" %>
    </div>

    <ul class="resources-list">
      <%= render @provinces %>
    </ul>

    <%= will_paginate renderer: BulmaPagination::Rails, :class => 'is-centered' %>
  </div>
</div>