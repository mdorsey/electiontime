<%= form_for(@social_media_type) do |f| %>
  <%= render 'shared/error_messages', object: f.object %>

  <div class="field">
    <%= f.label :name, "Name", class: 'label' %>
    <div class="control">
      <%= f.text_field :name, class: 'input' %>
    </div>
  </div>

  <div class="field">
    <%= f.label :website, "Website", class: 'label' %>
    <div class="control">
      <%= f.text_field :website, class: 'input' %>
    </div>
  </div>

  <div class="field">
    <%= f.label :icon, "Icon", class: 'label' %>
    <div class="control">
      <%= f.text_field :icon, class: 'input' %>
    </div>
  </div>

  <div class="buttons">
    <%= f.submit "Save", class: "button-with-extra-space is-primary" %>
    <%= link_to 'Cancel', cancel_path, class: "button-with-extra-space is-light", role: "button" %>
  </div>
<% end %>