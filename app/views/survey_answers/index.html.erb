<% provide(:title, 'Survey Answers') %>
<h1>Survey Answers</h1>

<div class="columns">
  <div class="column is-narrow">
    <%= render 'layouts/admin_menu' %>
  </div>
  <div class="column">
    <div class="buttons">
      <%= link_to '+ New Survey Answer', new_survey_answer_path, class: "button is-link" %>
    </div>

    <ul class="resources-list">
      <%= render @survey_answers %>
    </ul>

    <%= will_paginate renderer: BulmaPagination::Rails, :class => 'is-centered' %>
  </div>
</div>