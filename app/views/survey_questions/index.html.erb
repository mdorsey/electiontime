<% provide(:title, 'Survey Questions') %>
<h1>Survey Questions</h1>

<div class="buttons">
  <%= link_to '+ New Survey Question', new_survey_question_path, class: "button is-link" %>
  <%= link_to 'Back', admin_resources_path, class: "button" %>
</div>

<ul class="resources-list">
  <%= render @survey_questions %>
</ul>

<%= will_paginate renderer: BulmaPagination::Rails, :class => 'is-centered' %>