<% provide(:title, 'User Types') %>
<h1>User Types</h1>

<div class="buttons">
  <%= link_to '+ New User Type', new_user_type_path, class: "button is-link" %>
  <%= link_to 'Back', admin_resources_path, class: "button" %>
</div>

<ul class="resources-list">
  <%= render @user_types %>
</ul>

<%= will_paginate renderer: BulmaPagination::Rails, :class => 'is-centered' %>