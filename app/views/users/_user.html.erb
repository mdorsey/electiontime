<li>
  <%= link_to(user.first_name + ' ' + user.last_name, user) %>
  <% if current_user.user_type_id === UserType.find_by(name: 'Admin').id && !current_user?(user) %>
    | <%= link_to "delete", user, method: :delete, data: { confirm: "Are you sure?" } %>
  <% end %>
</li>